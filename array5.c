#include <stdio.h>
int main()
{
    int n,i,array[100];
    printf("\n Enter the number: ");
    
    for (i=0;i<=100;i++)
    {
        scanf("%d",&array[100]);
    }
    for(i=0;i<=100;i++)
    {
        printf("%d ", array[i]);
        if(i==100)
        {
            sqrt(array[i]);
        }
    }

    return 0;
}